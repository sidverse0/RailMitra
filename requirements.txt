fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
pydantic==2.9.2

# Optional — only required for the legacy indianrail.gov.in CAPTCHA-based
# PNR source (?source=indianrail). The default PNR source (confirmtkt) does
# NOT need these. Install them only if you need that legacy path, and note
# it also requires a system Tesseract OCR binary (`apt-get install
# tesseract-ocr` on Debian/Ubuntu, unavailable on some PaaS free tiers).
# pillow==10.4.0
# pytesseract==0.3.13

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
